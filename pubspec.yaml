name: random_walk
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  ui_components:
    path: common/ui_components
  ui_theming:
    path: common/ui_theming
  ui_utils:
    path: common/ui_utils 
  auth:
    path: auth
  flutter_svg: ^2.0.10+1
  firebase_core: ^3.6.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

